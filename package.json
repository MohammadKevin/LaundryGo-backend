{
  "name": "laundrygo-backend",
  "version": "1.0.0",
  "scripts": {
    "dev": "nodemon src/server.js",
    "build": "prisma generate",
    "start": "npx prisma migrate deploy && node src/server.js",
    "postinstall": "prisma generate"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.0.2",
    "prisma": "^5.22.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  }
}
